table:
  name: projects
  schema: public
array_relationships:
  - name: scans
    using:
      foreign_key_constraint_on:
        column: project_id
        table:
          name: scans
          schema: public
